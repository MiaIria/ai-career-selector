# 路径递进推演页复用模板

> 基于已完成的“升学深造（考研 / 保研）”页面提炼。体制内发展、市场化就业、自主发展三条路径均按本模板新增内容，不重新设计页面骨架。

## 1. 模板目标与边界

- 用途：以**静态、只读、可核验**的方式，逐节点解释一条发展路径。
- 目标：让学生看见每一步的时间、准入条件、能力、风险、成本与官方核验入口；不输出个人评分、成功率或结果承诺。
- 适用范围：一条主路径下的全部子赛道。例如体制内可拆为“公务员”“事业单位”，就业可拆为“校招”“实习转正”，自主发展可拆为“内容创作”“轻创业”。
- 非本模板范围：画像采集、AI 推荐、路径选择、决策保存、30 天任务生成与飞书同步。

## 2. 页面信息架构

```text
四轨推演概览卡
  └─ 子赛道 A「查看完整推演」
       └─ 递进推演页
            ├─ 返回四轨推演
            ├─ 路径标题、路径类型、说明与年度/规则提示
            ├─ 节点进度条（可直接点击任一节点）
            ├─ 当前节点卡
            │    ├─ 节点编号 / 总节点数 + 时间提示
            │    ├─ 节点标题与一句话目的说明
            │    ├─ 时间线 / 具备条件 / 所需能力 / 风险 / 成本
            │    └─ 官方资料与核验入口
            └─ 上一步 / 下一步（最后一步为“完成推演”）
```

## 3. 子赛道内容数据模板

每个子赛道单独提供一条 `Path` 数据。节点数建议 6—8 个；若存在硬性流程，可使用 7 个节点并按实际顺序排列。

```ts
type SourceLink = { label: string; url: string };

type PathNode = {
  title: string;
  time: string;
  summary: string;
  requirements: string[];
  capabilities: string[];
  cost: string | string[];
  risks: string[];
  sources: SourceLink[];
};

type Path = {
  title: string;
  subtitle: string;
  note: string;
  nodes: PathNode[];
};
```

### 3.1 字段写作规则

| 字段 | 必填要求 | 推荐写法 |
| --- | --- | --- |
| `title` | 节点名，4—12 字 | `岗位筛选`、`简历与作品集`、`首次付费验证` |
| `time` | 时间窗口、周期或阶段 | 多段内容用 `。` 或 `；` 分隔，页面会自动另起一行显示 |
| `summary` | 说明该节点要达成什么 | 一句话，不重复时间或卡片标题 |
| `requirements` | 进入节点前必须已经具备的事实或条件 | 优先写 `标题：正文`，标题会显示为绿色标签；无标题内容可直接写正文 |
| `capabilities` | 完成节点需要的能力 | 使用短词，如“信息检索”“材料表达”“面试沟通” |
| `risks` | 失败条件、错过窗口、外部不确定性 | 必须写成 `风险标题：后果或应对说明` |
| `cost` | 时间、金钱、机会成本 | 必须尽量写成 `时间：...`、`金钱：...`、`机会：...` |
| `sources` | 至少一个权威核验入口 | 优先官方系统、主管部门、目标机构规则页 |

### 3.2 新子赛道数据骨架

```ts
const NEW_TRACK_PATHS: Record<"subtrackA" | "subtrackB", Path> = {
  subtrackA: {
    title: "子赛道 A 完整推演",
    subtitle: "一句话描述这条路径的机制与适用阶段。",
    note: "具体时间、资格条件与规则以当年官方公告或目标机构要求为准。",
    nodes: [
      {
        title: "启动与信息核验",
        time: "建议从大二下至大三上启动；关键窗口为……。",
        summary: "确认投入方向、资格边界和当前准备度。",
        requirements: ["基础身份：……", "现实约束：……"],
        capabilities: ["信息检索", "自我评估", "规则核验"],
        cost: ["时间：……", "金钱：……", "机会：……"],
        risks: ["信息滞后：……", "方向误判：……"],
        sources: [{ label: "官方核验入口", url: "https://example.com" }],
      },
      // 按实际流程继续补齐 5—7 个节点。
    ],
  },
  subtrackB: {
    title: "子赛道 B 完整推演",
    subtitle: "……",
    note: "……",
    nodes: [],
  },
};
```

## 4. 五卡视觉模板

五张卡片顺序固定，不因路径不同而调整。这样用户跨路径阅读时可以形成稳定的阅读习惯。

| 顺序 | 卡片 | 背景 / 边框 | 标题色 | 内容规则 |
| --- | --- | --- | --- | --- |
| 1 | 时间线 | 浅蓝 `#eef6ff` / `#d7e7f5` | 蓝色 `#286486` | 按句号、分号自动分段；不使用标签 |
| 2 | 具备条件 | 浅绿 `#eff8f0` / `#d7ead9` | 绿色 `#2f6d42` | `标题：正文` 中的标题使用浅绿标签；无冒号内容保留正文 |
| 3 | 所需能力 | 浅青 `#eef9f7` / `#d3ebe6` | 青绿 `#1d7267` | 使用短词列表，不需要标签 |
| 4 | 风险 | 浅红 `#fff2f2` / `#f2d6d6` | 红色 `#a64949` | 每项均写 `风险标题：说明`；标题使用浅红标签 |
| 5 | 成本 | 浅黄 `#fff8e8` / `#f0e1bd` | 黄褐 `#8b681d` | 每项均写 `时间：...`、`金钱：...`、`机会：...`；标题使用浅黄标签 |

### 标签使用原则

- 标签只服务于“冒号前的类别词”，不可把整段文字放入标签。
- 标签颜色必须与所在卡片同色系，避免引入额外高饱和颜色。
- 风险标签允许使用浅红，但正文仍保持深灰，避免整卡造成警告感过强。

## 5. 版式与响应式规则

### 桌面端

- 推演内容容器最大宽度：`1760px`。
- 五卡等宽并排，卡片间距：`14px`。
- 节点卡内边距：上、左右 `26px`，底部 `20px`。
- 推荐字号：卡片标题 `14px`，正文 `12—13px`，行高 `1.55—1.7`。
- 以 `2048 × 990` 为桌面验收视口：允许路径标题区滚出视口；从**节点进度**到**五卡、资料入口、翻页控制**应能在同一视口内阅读。

### 平板与移动端

- 宽度不足时，五卡先切换为两列；小屏幕切换为一列。
- 节点进度条允许横向滚动，不折行压缩节点名称。
- 不缩小正文到低于可读范围；优先改列数、卡片间距与外层留白。

## 6. 交互模板

- 进入子赛道后，默认显示第一个节点。
- 顶部节点进度可点击跳转；已浏览节点显示完成态，当前节点显示高亮态。
- “上一步 / 下一步”只切换节点，不主动把页面滚到顶部，避免打断用户当前阅读位置。
- 最后一个节点的右侧按钮显示“完成推演”，点击后返回四轨概览。
- 返回概览时恢复用户离开概览前的滚动位置。
- 节点卡中的外部链接在新标签打开，并标识为“官方资料与核验入口”。

## 7. 三条待复用路径的内容规划建议

| 主路径 | 子赛道 | 建议节点链路 |
| --- | --- | --- |
| 体制内发展 | 公务员 | 信息核验 → 岗位筛选 → 笔试准备 → 报名与资格审查 → 笔试 → 面试 → 体检政审与录用 |
| 体制内发展 | 事业单位 | 信息核验 → 岗位筛选 → 专业/职测准备 → 报名与资格审查 → 笔试 → 面试/专业测试 → 体检考察与聘用 |
| 市场化就业 | 校招 | 方向定位 → 岗位能力盘点 → 简历与作品集 → 项目/实习补强 → 投递与笔试 → 面试 → Offer 比较与签约 |
| 市场化就业 | 实习转正 | 目标岗位筛选 → 简历投递 → 实习入职 → 试用期成果 → 转正评估 → 转正确认 / 校招备选 |
| 自主发展 | 内容创作 | 赛道与受众 → 账号定位 → 内容生产 → 发布与复盘 → 增长验证 → 商业化尝试 → 可持续性验证 |
| 自主发展 | 轻创业 | 问题选择 → 用户访谈 → 最小方案 → 首批用户 → 交付与反馈 → 首次付费 → 复购与持续收入验证 |

## 8. 新路径接入清单

- [ ] 在路径内容文件中新增两个子赛道的 `Path` 数据。
- [ ] 每个子赛道均具备 6—8 个真实流程节点。
- [ ] 每个节点都有五张信息卡和至少一个官方核验链接。
- [ ] 所有风险、成本，以及具备条件中可分类的内容，均使用 `标题：正文` 格式。
- [ ] 不写精确成功率，不承诺录取、上岸、Offer 或收入。
- [ ] 时间、资格、报名、考试、招聘等可变信息均标明“以当年官方公告为准”。
- [ ] 四轨概览卡新增两个子赛道入口，入口文案采用“查看 XXX 完整推演”。
- [ ] 在 2048 × 990、平板和手机宽度下核验布局。
- [ ] 运行 `npm test`、`npm run lint`，并完成页面视觉检查。

## 9. 实施文件映射

| 文件 | 责任 |
| --- | --- |
| `src/lib/four-track-content.ts` | 路径、节点、官方来源等静态内容数据 |
| `src/components/sandbox-app.tsx` | 子赛道入口、递进节点交互、文本分段与冒号标签化渲染 |
| `src/app/globals.css` | 五卡语义配色、桌面/移动端布局与标签样式 |

新增路径时优先只扩展数据和入口；除非现有模板无法表达该路径的真实规则，否则不要另起一套页面结构或配色体系。
